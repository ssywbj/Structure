<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>-->
    </style>

    <style name="ThemeNoActionBar" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="ThemeBrandA" parent="AppTheme">
        <item name="brand_color">@color/brand_color_a</item>
    </style>

    <style name="ThemeBrandB" parent="AppTheme">
        <item name="brand_color">@color/brand_color_b</item>
    </style>
</resources>
