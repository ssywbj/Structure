<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Structure">

        <activity
            android:name=".ComposeActivity"
            android:exported="true"
            android:theme="@style/Theme.Structure">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <service
            android:name=".service.ComposeWallpaper"
            android:exported="true"
            android:label="Compose Wallpaper"
            android:permission="android.permission.BIND_WALLPAPER">

            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>

            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/compose_wallpaper_config" />
        </service>

        <receiver
            android:name=".receiver.ComposeWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/compose_widget_config" />
        </receiver>

        <activity
            android:name=".MixUiActivity"
            android:label="MixUi"
            android:theme="@style/Theme.DarkActionBar" />

        <activity android:name=".ListGridActivity" />
    </application>

</manifest>