#include <stdio.h>

int main()
{
    //http://c.biancheng.net/view/1990.html
    printf("This is a C program.\n");

    //Linux、Windows(安装gcc)
    //编译命令:gcc Xxx.c, 生成文件a.out(windows:a.exe), 执行命令:./a.out(windows:./a.exe)
    //编译命令:gcc -o name Xxx.c, 生成name文件, 执行命令:./name

    //Mac
    //把gcc换成自带的clang命令

    return 0;
}
